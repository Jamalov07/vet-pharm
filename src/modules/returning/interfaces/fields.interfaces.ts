import { ReturningModel } from '@prisma/client'

export declare interface ReturningRequired extends Required<ReturningModel> {}

export declare interface ReturningOptional extends Partial<ReturningModel> {}
